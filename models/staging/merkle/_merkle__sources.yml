version: 2

sources:
  - name: merkle
    database: churn_demo
    schema: raw
    loader: snowflake # informational only (free text)
    
    tags:
      - snowflake
      
    tables:
      - name: merkle_datasource